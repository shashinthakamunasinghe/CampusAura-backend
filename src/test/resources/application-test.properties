# Test Profile Configuration
# Firebase is disabled during tests to avoid requiring credentials

# Disable Firebase initialization for tests
firebase.enabled=false

# Test server port
server.port=8081

# Test logging
logging.level.com.example.campusaura=DEBUG
